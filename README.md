# 马年彩单页系统

这是一个纯前端单页工具，用于投注录入、风险/赔付计算、开奖校验和 AI 识别汇总录入。

## 使用

直接打开 `index.html`，或通过 GitHub Pages 访问部署后的页面。

## 注意

- DeepSeek API Key 不写入源码，需要在页面的“AI识别录入”弹窗中手动填写。
- 数据保存在当前浏览器的 `localStorage` 中，不会自动同步到其他设备。
- 请自行确认使用场景的合规性。
